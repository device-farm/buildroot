config BR2_DEVICE_FARM
	bool "device.farm connected device"
	depends on BR2_PACKAGE_WIREGUARD_TOOLS
	depends on BR2_PACKAGE_IPROUTE2
	depends on BR2_PACKAGE_IPTABLES
	depends on BR2_PACKAGE_DOCKER_ENGINE	
	help
	  This is a meta package for dependencies required to connect the device to DEVICE.FARM.

	  http://www.device.farm

comment "device-farm needs wireguard tools"
	depends on !BR2_PACKAGE_WIREGUARD_TOOLS

comment "device-farm needs iproute2"
	depends on !BR2_PACKAGE_IPROUTE2

comment "device-farm needs iptables"
	depends on !BR2_PACKAGE_IPTABLES

comment "device-farm needs docker-engine"
	depends on !BR2_PACKAGE_DOCKER_ENGINE

